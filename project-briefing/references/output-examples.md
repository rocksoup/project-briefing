# Output Examples

Use these examples to calibrate tone and shape. Keep the output short unless the user asks for a longer narrative.

## Example 1: Tracker-Backed Engineering Project

`Project Objective:` Build a hosted data platform that can ingest tennis data, support remote agent workflows, and eventually produce higher-level intelligence and content.

`Completed Recently:` The Drupal platform and the first Power Five ingestion pipeline are complete. Recent work focused on proving that browser-based cloud coding sessions can reach locked Pantheon Dev and complete the required JSON:API smoke tests.

`Current Milestone:` Turn that proven cloud access path into the standard agent runtime by operationalizing the real agent workspace and validating the end-to-end workflow.

`Next Concrete Step:` Validate the same remote workflow from the second cloud coding provider so the operating model is genuinely portable.

`Risks / Open Questions:` The tracker may lag behind the most recent validations, and portability outside the primary cloud workflow still needs explicit confirmation.

## Example 2: Git-And-Docs Fallback

`Project Objective:` Modernize the internal analytics app and reduce manual reporting work.

`Completed Recently:` The codebase shows recent work on the new reporting pipeline, deployment scripts, and operations docs. There is no formal tracker in the repo, so this summary is inferred from git activity and documentation.

`Current Milestone:` The project appears to be transitioning from infrastructure setup into operational rollout of the new reporting flow.

`Next Concrete Step:` Confirm the production-readiness checklist and validate the end-to-end reporting run in the hosted environment.

`Risks / Open Questions:` Without a formal backlog, it is harder to tell whether the implementation state and project priorities still match.

## Example 3: Tracker-Git Mismatch

`Project Objective:` Launch a self-serve onboarding flow for customers.

`Completed Recently:` The tracker still shows the onboarding integration as open, but recent merged work suggests most of the implementation has landed.

`Current Milestone:` The project is likely in final verification rather than active build-out.

`Next Concrete Step:` Reconcile the tracker with the shipped code and run the remaining acceptance checks.

`Risks / Open Questions:` The formal project record appears stale, so the current tracker state may overstate the remaining work.

## Example 4: Longer Narrative On Request

If the user asks for more depth, expand each section into a short paragraph, but keep the same section order and business language.
